<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.trafood.trafood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.  g
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailRm"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".DetailMenu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GalleryRm"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProfilGuest"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProfilVote"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UserArticle"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SearchActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LocationActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LocationSearch"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".FilterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingProfilActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditProfilActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditKedaiActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TambahMenuActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RingkasanKedaiActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditMenuActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditFotoMenuActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".KedaiEditFotoActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".KedaiEditPetaActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditPetaActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChangePasswordActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditFotoUser"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DaftarKedaiActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DaftarKedaiInfo"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DaftarKedaiAlamat"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DaftarKedaiFoto"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DaftarKedaiPeta"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TesLoginGoogle"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ReviewKedaiActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RatingActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CartActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CourierActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ConfirmationActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MenuLainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AlamatActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InputMapActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".RiwayatAlamatActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>